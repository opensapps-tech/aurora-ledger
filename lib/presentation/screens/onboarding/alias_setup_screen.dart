import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// TODO: Implement AliasSetupScreen
class AliasSetupScreen extends ConsumerWidget {
  const AliasSetupScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('AliasSetupScreen')),
      body: const Center(child: Text('TODO: implement AliasSetupScreen')),
    );
  }
}
